Put the BDTuition logo image here named exactly:  logo.png

This folder is registered in pubspec.yaml (flutter: assets:).
The home/welcome screen loads assets/logo.png.
If logo.png is missing, the app falls back to the old icon + "BDTuition" text.
